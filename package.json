{
  "name": "recesso",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/client",
    "packages/server",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run build -w @recesso/shared && concurrently \"npm run dev -w @recesso/client\" \"npm run dev -w @recesso/server\"",
    "build": "npm run build -w @recesso/shared && npm run build -w @recesso/client && npm run build -w @recesso/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
